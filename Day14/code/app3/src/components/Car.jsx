function Car({ company, model, year }) {
  return (
    <div>
      <div>model = {model}</div>
      <div>year = {year}</div>
      <div>company = {company}</div>
      <hr />
    </div>
  )
}

export default Car
